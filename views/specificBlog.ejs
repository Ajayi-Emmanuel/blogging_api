<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<style>
    body{
            padding: 0;
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }
        :root{
            --small-padding: 5px;
            --medium-padding: 10px;
            --margin-top: 10px;
            --border-radius: 5px;
        }
        nav{
            background-color: rgb(28, 28, 55);
            padding: var(--medium-padding);
            display: flex;
            flex-direction: row;
            justify-content: space-between;
        }
        header{
            text-decoration: none;
            color: white;
            font-size: 25px;
            font-weight: bold;
        }
        nav button{
            width: 70px;
            padding: 10px;
            background-color: white;
            border: none;

        }
        .container{
            width: 1000px;
            max-width: 100%;
            border: 1px solid rgb(115, 113, 113);
            margin: 20px auto;
        }
        nav a{
            color: white;
            text-decoration: none;
        }
</style>
<body>
    <nav>
        <header><a href="/blog/">My Blogg</a></header>
        
    </nav>

    <div class="container">
        <p>published at: <%= blog.timestamp.createdAt %></p>
        <h2><%= blog.title %></h2>
        <p><%= blog.body %></p>
    </div>
</body>
</html>